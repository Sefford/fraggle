ext {
    versionMajor = 1
    versionMinor = 7
    versionPatch = 1
    versionBuild = 0
}

group = 'com.sefford.fraggle'
version = "${versionMajor}.${versionMinor}.${versionPatch}"

description = 'fraggle'

task wrapper(type: Wrapper) {
    gradleVersion = '3.0'
}